# IPv4

```bash
"${PYROLYSE_PATH}/tools/script/scrapping/scrap_ip_triplet_reassembly_algorithm_consistency_scenarii.sh" \
"${PYROLYSE_PATH}/target/os/${TARGET_NAME}" \
ipv4
```

# IPv6

```bash
"${PYROLYSE_PATH}/tools/script/scrapping/scrap_ip_triplet_reassembly_algorithm_consistency_scenarii.sh" \
"${PYROLYSE_PATH}/target/os/${TARGET_NAME}" \
ipv6
```

# TCP

```bash
"${PYROLYSE_PATH}/tools/script/scrapping/scrap_tcp_triplet_reassembly_algorithm_consistency_scenarii.sh" \
"${PYROLYSE_PATH}/target/os/${TARGET_NAME}"
```